
/*class AgrimedDepShowcase extends StatelessWidget {
  const AgrimedDepShowcase({
    super.key,
    required this.darkMode,
  });

  final bool darkMode;

  @override
  Widget build(BuildContext context) {
    return AgrimedRoundedContainer(
      showBorder: true,
      borderColor: AgrimedColors.darkGrey,
      backgroundColor: Colors.transparent,
      margin: const EdgeInsets.only(bottom: AgrimedSize.spaceBtwItems),
      child: Column(
        children: [
          /// categories with claim count

          Row(
            children: [
              Expanded(
                child: AgrimedRoundedContainer(
                  height: 130,
                  backgroundColor: darkMode ? AgrimedColors.black : AgrimedColors.light,
                  margin: const EdgeInsets.only(right: AgrimedSize.sm),
                  padding: const EdgeInsets.all(AgrimedSize.md),
                  child: const Image(fit: BoxFit.contain,image: AssetImage(AgrimedImages.prod)),
                  


                ),
              ),
            ],
          )
        ],


      ),
    );
  }
}
*/
